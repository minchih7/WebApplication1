using Microsoft.AspNetCore.Mvc;
using NuGet.Protocol;

namespace WebApplication1.Controllers
{
    public class GETPController : Controller
    {
        //public IActionResult GETP()
        //{
        //    var databasePassword = Environment.GetEnvironmentVariable("DATABASE_PASSWORD");

        //    if (databasePassword == null)
        //    {
        //        return Json("Environment variable not set");
        //    }

        //    return Json(databasePassword);
        //}
    }
}
